# Use Ctrl+A as the prefix key
set -g prefix C-a
unbind C-b
# Press the prefix twice to send Ctrl+A to the active program.
bind C-a send-prefix

# Use Vim shortcuts
setw -g mode-keys vi

# Make `Ctrl+A r` reload the config file
unbind r
bind r source-file ~/.tmux.conf
